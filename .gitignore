# ############
# Wordpress Core (see project section to ignore specific module)
# ############

# ignore everything in the root except the "wp-content" directory.
public/*
!public/wp-content/
public/wp-content/plugins/*
public/wp-content/languages/*
!public/wp-content/*/index.php

# ignore all files starting with .
.*
!.env.sample

# track this file .gitignore (i.e. do NOT ignore it)
!/.gitignore
!/.gitattributes

# track .editorconfig file (i.e. do NOT ignore it)
!public/.editorconfig

# track readme.md in the root (i.e. do NOT ignore it)
!public/readme.md

# track samples for config
!public/*sample*

# ignore all files that start with ~
public/~*

# ignore OS generated files
ehthumbs.db
Thumbs.db

# ignore Editor files
*.sublime-project
*.sublime-workspace
*.komodoproject

# ignore log files and databases
public/*.log
public/*.sql
public/*.sqlite

# ignore compiled files
public/*.com
public/*.class
public/*.dll
public/*.exe
public/*.o
public/*.so

# ignore packaged files
public/*.7z
public/*.dmg
public/*.gz
public/*.iso
public/*.jar
public/*.rar
public/*.tar
public/*.zip

# ignore everything in the "wp-content" directory, except:
# "mu-plugins" directory
# "plugins" directory
# "themes" directory
public/wp-content/*
!public/wp-content/mu-plugins/
!public/wp-content/plugins/
!public/wp-content/themes/
!public/wp-content/languages/

# ignore these plugins
public/wp-content/plugins/hello.php

# ignore specific themes
public/wp-content/themes/twenty*/

# ignore node/grunt dependency directories
public/node_modules/

# ############
# Project
# ############
docker/local.yml
/vendor
# Wordpress plugins that can't be downloaded through wppackagist
# !public/wp-content/plugins/plugin-name

# ############
# Windows
# ############
# Windows thumbnail cache files
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
# Dump file
*.stackdump
# Folder config file
[Dd]esktop.ini
# Recycle Bin used on file shares
$RECYCLE.BIN/
# Windows Installer files
*.cab
*.msi
*.msix
*.msm
*.msp
# Windows shortcuts
*.lnk

# ############
# Linux
# ############
*~
# temporary files which can be created if a process still has a handle open of a deleted file
.fuse_hidden*
# KDE directory preferences
.directory
# Linux trash folder which might appear on any partition or disk
.Trash-*
# .nfs files are created when an open file is removed but is still being accessed
.nfs*

# ############
# MacOsx
# ############
# General
.DS_Store
.AppleDouble
.LSOverride
# Icon must end with two \r
Icon
# Thumbnails
._*
# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk